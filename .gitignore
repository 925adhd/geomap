node_modules/
.next/
out/
.env
.env.local
.env*.local
*.tsbuildinfo
next-env.d.ts
.DS_Store
Thumbs.db

# Local lead/scan/usage stores. These contain real customer emails
# (leads.json) and historical Places API spend (usage.json). Never commit.
data/

# Claude Code local settings
.claude/

# Local-only project notes that we don't want public on GitHub.
SUPABASE.md

# Local backup of the original sample heatmap before cropping. Kept on
# disk so we can re-crop, but not worth tracking in git.
public/audit-sample-heatmap-original.webp
